import React from "react";
// import CardComponent from '../components/CardComponent'

const StudentPage = ({ name, age, classs, department, email, id }) => {
  {
    // console.log(id);
  }
  // const {id} = useParams()
  return (
    <>
      <p>id ===== {{id}}</p>
    </>
  );
};

export default StudentPage;
